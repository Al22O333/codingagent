"""Command-line entry point for the Coding Agent package."""

STARTUP_MESSAGE = "Coding Agent v1"


def main() -> int:
    """Start the minimal Step 0 command-line application."""
    print(STARTUP_MESSAGE)
    return 0
